#include <bits/stdc++.h>
using namespace std;
#define endl '\n'
#define optimize()                    \
    ios_base::sync_with_stdio(false); \
    cin.tie(NULL);                    \
    cout.tie(NULL);

void ans()
{

    int n, m;
    cin >> n >> m;

    for (int i = 0; i < n; i++)
    {
        for (int j = 0; j < m; j++)
        {
        }
    }

    cout << c << endl;
}
int main()
{
    optimize();
    ans();

    return 0;
}

//https: // atcoder.jp/contests/abc237/tasks/abc237_b?lang=en