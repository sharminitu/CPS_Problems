#include <bits/stdc++.h>
using namespace std;
#define endl '\n'
#define optimize()                    \
    ios_base::sync_with_stdio(false); \
    cin.tie(NULL);                    \
    cout.tie(NULL);

void ans()
{
    int n;
    cin >> n;

    string s;
    cin>>s;
    cout<<s.back()<<endl;
}
int main()
{
    optimize();

    int test;
    cin >> test;
    while (test--)
    {
        ans();
    }
}
